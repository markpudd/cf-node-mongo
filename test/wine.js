var expect    = require("chai").expect;
describe('Node Wine', function() {
  describe('addWine()', function () {
    it('should add a wine', function () {
      expect(1+1).to.equal(2);
    });
  });
});
